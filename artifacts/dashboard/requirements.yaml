packages:
  "framer-motion": "For page transitions, list animations, and micro-interactions"
  "lucide-react": "Beautiful consistent icons for the dashboard"
  "date-fns": "For formatting conversation and booking timestamps"
  "recharts": "For dashboard analytical charts and trends"
  "react-hook-form": "For robust form state management in inventory and settings"
  "@hookform/resolvers": "For Zod validation integration with react-hook-form"
  "zod": "For client-side form schema validation"
  "clsx": "For conditional class merging in reusable components"
  "tailwind-merge": "For conflict-free Tailwind class merging"
  "zustand": "For lightweight global state like language preference"

images: []

notes:
  - The application relies on React Query hooks generated by Orval in @workspace/api-client-react
  - Full RTL (Right-to-Left) support is managed via a global store and the HTML dir attribute
  - Dark mode is forced by default for a premium professional aesthetic
